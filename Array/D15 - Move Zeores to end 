//Brute - create duplicate array and store


// Optimal 
void pushZerosAtEnd(vector<int> &arr) 
{
	// Write your code here.
	int n = arr.size();
	if(n==1) return;
	int j = 0;
	for(int i=0;i<n;i++){
		if(arr[i]!=0) {
			swap(arr[i],arr[j]);
			j++;
		}
	}
}